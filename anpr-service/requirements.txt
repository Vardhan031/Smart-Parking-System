ultralytics==8.4.14
# easyocr==1.7.2  # Optional: legacy OCR backend
opencv-python-headless==4.13.0.92
fastapi==0.129.0
uvicorn[standard]==0.40.0
python-multipart==0.0.22
numpy==2.4.2
# Video pipeline — ONNX Runtime (~3-5x faster than PyTorch on CPU)
onnxruntime==1.20.1
# Video pipeline — WebSocket support
websockets==12.0
# PaddleOCR — primary OCR backend (~3-4x faster & more accurate than EasyOCR)
paddlepaddle==3.2.2
paddleocr==2.10.0
